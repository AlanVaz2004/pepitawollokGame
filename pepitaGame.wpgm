import pepita.*
import alpiste.*
import silvestre.*

import direcciones.*
import wollok.game.*

program PepitaGame {
	game.title("El titulo")
	game.height(10)
	game.width(10)
	game.boardGround("fondo2.jpg")

	game.addVisual(pepita)
	//game.addVisual(alpiste)
	game.addVisual(silvestre)

	keyboard.left().onPressDo({ pepita.mover(izquierda)})
	keyboard.right().onPressDo({ pepita.mover(derecha)})
	keyboard.up().onPressDo({ pepita.mover(arriba)})
	keyboard.down().onPressDo({ pepita.mover(abajo)})

	keyboard.left().onPressDo({ silvestre.mover(izquierda)})
	keyboard.right().onPressDo({ silvestre.mover(derecha)})
	
	const tick = game.tick(2000, { pepita.mover(abajo) }, false)
  	tick.start()

	 // Cuando el personaje colisione con la caja, el personaje habla y la caja se desplaza
  	game.onCollideDo(pepita, { silvestre =>
    //elemento.subir()
    game.say(pepita, "Opa! Me llevé choque con silvestre!")
	pepita.estaViva(false)
  	})

	game.start()
}
